<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Consulta</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
  </head>
  <div class="card">
    <div class="card-header">Consulta</div>
    <div class="card-body">
      <h4>Paciente: <%= appo.name %></h4>
      <h4>CPF: <%= appo.cpf %></h4>
      <h4>E-mail: <%= appo.email %></h4>
      <h4>Descrição: <%= appo.description %></h4>
      <h4>
        Data: <%= ` ${appo.date.getDate()+1}/ ${appo.date.getMonth()}/
        ${appo.date.getFullYear()} `%>
      </h4>
      <h4>Hora: <%= appo.time %></h4>
      <form action="/finish" method="post">
        <input type="hidden" name="id" value="<%= appo._id %>">
        <button>Finalizar consulta</button>
      </form>
    </div>
  </div>
  <body></body>
</html>
